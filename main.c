#include <limits.h>
#include <stdio.h>
#include "main.h"

/**
 * main - Entry point
 *
 * Return: Always 0
 */
int main(void)
{	
	int a = -1024;
	_printf("max = %i\n",a);
	printf("max = %i\n",a);
	return (0);

}
