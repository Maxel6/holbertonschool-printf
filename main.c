#include <limits.h>
#include <stdio.h>
#include "main.h"

/**
 * main - Entry point
 *
 * Return: Always 0
 */
int main(void)
{
	int max = 0;
	int min = INT_MIN;
	_printf("max = %d\n", max);
	_printf("min = %d\n", min);
	return (0);

}
